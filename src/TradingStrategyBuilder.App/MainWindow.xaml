<Window x:Class="TradingStrategyBuilder.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trading Strategy Builder - Phase 1 POC" Height="700" Width="1200">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
            Trading Strategy Builder - Phase 1 Proof of Concept
        </TextBlock>

        <!-- Input Section -->
        <GroupBox Grid.Row="1" Header="Natural Language Input" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Margin="0,0,0,5">
                    Enter your trading strategy description:
                </TextBlock>
                
                <TextBox x:Name="InputTextBox" Grid.Row="1" 
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12" Margin="0,0,0,10"/>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button x:Name="BuildButton" Content="Build Strategy" 
                            Click="BuildButton_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" 
                               Foreground="Gray" Text="Ready"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Results Section -->
        <GroupBox Grid.Row="3" Header="Results">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TabControl Grid.Row="1">
                    <TabItem Header="IR (Intermediate Representation)">
                        <TextBox x:Name="IRTextBox" IsReadOnly="True" 
                                 TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                    </TabItem>
                    <TabItem Header="Compiled Signals (JSON)">
                        <TextBox x:Name="SignalsTextBox" IsReadOnly="True" 
                                 TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                    </TabItem>
                    <TabItem Header="Validation">
                        <TextBox x:Name="ValidationTextBox" IsReadOnly="True" 
                                 TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                    </TabItem>
                </TabControl>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
